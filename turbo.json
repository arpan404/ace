{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "PORT",
    "VITE_WS_URL",
    "VITE_DEV_SERVER_URL",
    "ELECTRON_RENDERER_PORT",
    "ACE_LOG_WS_EVENTS",
    "ACE_MODE",
    "ACE_PORT",
    "ACE_HOST",
    "ACE_NO_BROWSER",
    "ACE_AUTO_BOOTSTRAP_PROJECT_FROM_CWD",
    "ACE_HOME",
    "ACE_AUTH_TOKEN",
    "ACE_RELAY_URL",
    "ACE_DAEMONIZED",
    "ACE_DESKTOP_WS_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "dist-electron/**"]
    },
    "dev": {
      "dependsOn": ["@ace/contracts#build"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": [],
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": []
    }
  }
}
