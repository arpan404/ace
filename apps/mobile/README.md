# ace mobile

Reserved for the future mobile client.

Mobile will connect to the desktop host through the shared `ace-protocol`
contract. It must not become a second owner of provider process state.

